{
  "scripts": {
    "test": "npm run lint && npm run clean && tsc && ava",
    "lint": "tslint -c tslint.json src/**/*.ts test/**/*.ts",
    "clean": "find src test -type f -name '*.js' -exec rm -rf {} \\;"
  },
  "devDependencies": {
    "ava": "^0.20.0",
    "tslint": "^5.5.0",
    "tslint-config-standard": "^6.0.1",
    "typescript": "^2.4.1"
  }
}
